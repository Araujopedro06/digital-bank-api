spring:
  application:
    name: digital-bank-api
  profiles:
    default: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

spring.servlet.multipart:
  max-file-size: 2MB
  max-request-size: 3MB

app:
  face:
    # Euclidean distance between face-api.js descriptors below which two faces are
    # taken to be the same person. The library's general-purpose default is 0.6;
    # this is tightened for a banking flow, trading retries for fewer false accepts.
    match-threshold: 0.5
  jwt:
    # Override in every real environment. Must be at least 32 bytes for HS256.
    secret: ${JWT_SECRET:change-me-in-production-this-is-a-dev-only-secret-key}
    expiration: 2h
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:4200}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:bank;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
